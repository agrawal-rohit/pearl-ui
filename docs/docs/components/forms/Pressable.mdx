---
sidebar_position: 1
title: Pressable
---

import SourceButton from "../../../src/components/SourceButton/SourceButton";

<div style={{ display: "flex" }}>
  <SourceButton
    label="Source"
    href="https://github.com/agrawal-rohit/pearl-ui/blob/main/src/components/atoms/pressable/pressable.tsx"
  />
</div>

The `Pressable` is a functional component use to make interactive elements such as buttons, links, and more.

## Importing the Component

## Import

```jsx
import { Pressable } from "pearl-ui";
```

## Usage

```jsx
<Pressable
  p="4"
  onPress={() => {
    console.log("I was pressed!");
  }}
  backgroundColor="primary.500"
>
  <Text>Press me!</Text>
</Pressable>
```

### Customizing the Android Ripple Effect

The `Pressable` component provides a default ripple effect for Android devices. This effect can be customized using the `androidRippleConfig` prop or disabled entirely with the `isDisabledAndroidRipple` prop. See the examples below for more details:

```jsx
// Customizing the ripple effect
<Pressable
  p="4"
  onPress={() => {
    console.log("I was pressed!");
  }}
  backgroundColor="primary.500"
  androidRippleConfig={{ color: "primary.800" }}
>
  <Text>Press me!</Text>
</Pressable>

// Disabling the ripple effect
<Pressable
  p="4"
  onPress={() => {
    console.log("I was pressed!");
  }}
  backgroundColor="primary.500"
  isDisabledAndroidRipple
>
  <Text>Press me!</Text>
</Pressable>
```

## Example

import Snack from "../../../src/components/ExpoSnack";

<Snack snackId="@agrawal-rohit/pressable-light" />

## Accessibility

- `Pressable` has the `role` of `button`.
- `Pressable` has the default label set as 'Press me!'. A custom label can be specified using the `accessibilityLabel` prop.
- `Pressable` expects an `actionDescription` prop to specify the intented outcome of interacting with the component eg. 'Closes modal', 'Go to the homepage', etc.

## Component Properties

### Supported Styling Properties

`Pressable` supports all properties related to the [Box](../layout/Box) component.

### Additional Properties

| Name                      | Required | Type                                                                                              | Default                                      | Description                                                                                                                     |
| ------------------------- | -------- | ------------------------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `onPress`                 | No       | <t>({ nativeEvent: [PressEvent](https://reactnative.dev/docs/pressevent) }) => void</t>           |                                              | Function called when the component is pressed.                                                                                  |
| `onPressIn`               | No       | <t>({ nativeEvent: [PressEvent](https://reactnative.dev/docs/pressevent) }) => void</t>           |                                              | Called immediately when a touch is engaged, before `onPressOut` and `onPress`.                                                  |
| `onPressInDelay`          | No       | <t>number</t>                                                                                     | `null`                                       | Duration (in milliseconds) to wait after press down before calling onPressIn.                                                   |
| `onPressOut`              | No       | <t>({ nativeEvent: [PressEvent](https://reactnative.dev/docs/pressevent) }) => void</t>           |                                              | Called when a touch is released.                                                                                                |
| `onLongPress`             | No       | <t>({ nativeEvent: [PressEvent](https://reactnative.dev/docs/pressevent) }) => void</t>           |                                              | Called if the time after onPressIn lasts longer than 500 milliseconds. This time period can be customized with `delayLongPress` |
| `delayLongPress`          | No       | <t>number</t>                                                                                     | `500`                                        | Duration (in milliseconds) from onPressIn before onLongPress is called                                                          |
| `hitSlop`                 | No       | <t>[Rect](https://reactnative.dev/docs/rect) \| number</t>                                        |                                              | Sets additional distance outside of element in which a press can be detected.                                                   |
| `pressRetentionOffset`    | No       | <t>[Rect](https://reactnative.dev/docs/rect) \| number</t>                                        | { bottom: 30, left: 20, right: 20, top: 20 } | Additional distance outside of this view in which a touch is considered a press before onPressOut is triggered.                 |
| `isDisabled`              | No       | <t>boolean</t>                                                                                    | `false`                                      | If true, the press behavior is disabled.                                                                                        |
| `isDisabledAndroidSound`  | No       | <t>boolean</t>                                                                                    | `false`                                      | If true, Android system sound on press is not played.                                                                           |
| `androidRippleConfig`     | No       | <t>{ color: PearlTheme["palette"], borderless: boolean, radius: number, foreground: boolean }</t> | { color: "#E4E9F2", borderless: false }      | Configures the ripple effect for the android_ripple property.                                                                   |
| `isDisabledAndroidRipple` | No       | <t>boolean</t>                                                                                    | `false`                                      | If true, the Android ripple effect is enabled and its properties are configured.                                                |
| `actionDescription`       | No       | <t>string</t>                                                                                     |                                              | A short description of the action that occurs when this element is interacted with (Used for accessibility).                    |
| `_pressed`                | No       | <t>object</t>                                                                                     | `{}`                                         | Style properties that are applied when the component is in a pressed state.                                                     |
| `_disabled`               | No       | <t>object</t>                                                                                     | `{}`                                         | Style properties that are applied when the component is in a disabled state.                                                    |
